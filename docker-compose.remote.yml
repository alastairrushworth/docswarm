services:
  ollama-main:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  developer-agent:
    environment:
      - DOCSWARM_MODE=remote
